Simple application to read keypress events.

Set KEYBOARD_DEVICE in logger.h to the relevant /dev/input node.

Needs to be run as root. Or the permissions of the node need to be set accordingly.

Start policyserve.sh before starting PedoFile.
